{
  "name": "releases",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "./node_modules/.bin/pm2 start ecosystem.config.js",
    "ls": "./node_modules/.bin/pm2 ls",
    "stop": "./node_modules/.bin/pm2 delete release",
    "test": "export NODE_TLS_REJECT_UNAUTHORIZED='0' && ts-node -r dotenv/config ./src/index.ts",
    "i": "rm -rf node_modules && npm i && ./node_modules/.bin/pm2 install typescript",
    "tsc": "npm run clean && tsc",
    "clean": "rm -rf build",
    "pub": "npm run tsc && npm publish",
    "build": "rm -rf build && tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.25.0",
    "concat-stream": "^2.0.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "node-cron": "^3.0.0",
    "pg": "^8.7.3",
    "pm2": "^5.2.0",
    "rss-parser": "^3.12.0",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.16.11",
    "@babel/preset-typescript": "^7.16.7",
    "@types/node": "^17.0.16"
  }
}
